name: Node.js CI - Lint, Security, Build & Push

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 3️⃣ Install dependencies (for lint & test only)
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Run ESLint
      - name: Run ESLint
        run: npm run lint

      # 5️⃣ Run tests
      - name: Run tests
        run: npm test

      # 6️⃣ Dependency security scan
      - name: NPM audit (high & critical only)
        run: npm audit --audit-level=high

      # 7️⃣ Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 8️⃣ Set image tag (short commit SHA)
      - name: Set image tag
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      # 9️⃣ Build Docker image (NO CACHE, final runtime image)
      - name: Build Docker image
        run: |
          docker build --no-cache \
            -t ${{ secrets.DOCKER_USERNAME }}/node-ci-flow:${IMAGE_TAG} \
            -t ${{ secrets.DOCKER_USERNAME }}/node-ci-flow:latest .

      # 🔟 Scan FINAL Docker image with Trivy
      - name: Scan Docker image for vulnerabilities
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/node-ci-flow:${{ env.IMAGE_TAG }}
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: 1
          ignore-unfixed: true

      # 1️⃣1️⃣ Push Docker images
      - name: Push Docker images
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/node-ci-flow:${IMAGE_TAG}
          docker push ${{ secrets.DOCKER_USERNAME }}/node-ci-flow:latest
